# 기존 git 초기화가 되어있다면 삭제
rm -rf .git

# 새로 git 초기화 및 원격 저장소 연결
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/Myunghee-Yoon/GrowMe.git
git push -u origin main